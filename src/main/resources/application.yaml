spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library_db
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:db/changelog.xml
  jpa:
    show-sql: true
    open-in-view: false